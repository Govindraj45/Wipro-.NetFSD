# W4 Day 2 - Razor Pages and MVC Model Binding

This folder contains two focused apps:

- `AdvancedRazorCatalog`: complex model binding with product categories, partial rendering, and custom Razor Page routes like `/products/{id}` and `/categories/{slug}`
- `MvcBindingWorkshop`: MVC model binding for simple fields and nested address data with a submitted summary view
